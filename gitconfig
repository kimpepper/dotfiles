[user]
	name = Kim Pepper
	email = kim@pepper.id.au

[diff]
    renames = copies
    color = auto
 
[color]
	branch = auto
	diff = auto
	status = auto
    ui = true
	interactive = auto
 
[web]
    browser = chrome

[alias]
    co = checkout
    s = status
    c = commit
    d = diff
    desc = describe --tags
    a = add
    b = branch
    f = fetch
    l = log --decorate --graph 
    m = merge
    p = push
    r = rebase
    re = reset
    sh = show
    t = tag
    up = pull --rebase
    changelog = log --no-merges --pretty=format:'* %s - %an'
[instaweb]
    local = true
    httpd = /usr/sbin/httpd
    port = 4321
    browser = firefox
    modulepath = /usr/libexec/apache2
[core]
    editor = vim
	excludesfile = /Users/kim/.gitignore_global
	filemode = false
[merge]
    tool = opendiff

[push]
	default = current

[github]
	user = kimpepper
[credential]
	helper = osxkeychain
